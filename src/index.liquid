---
layout: base.liquid
title: "Hello!"
---

<div class="home">
    <header class="hero">
        <h1 class="hero-title">ilakli</h1>
        <p class="hero-description">
            Welcome to my blog! This is a simple, beautiful space where I share my thoughts and ideas.
        </p>
    </header>

    <section class="posts-section">
        <div class="posts-list">
            {% assign sorted_posts = collections.posts | sort: 'date' | reverse %}
            {% for post in sorted_posts %}
            <article class="post-preview">
                <time class="post-preview-date">{{ post.data.pubDate | date: "%B %d, %Y" }}</time>
                <h3 class="post-preview-title">
                    <a href="{{ post.url }}">{{ post.data.title }}</a>
                </h3>
                {% if post.data.description %}
                <p class="post-preview-description">{{ post.data.description }}</p>
                {% endif %}
            </article>
            {% endfor %}
        </div>
    </section>
</div>